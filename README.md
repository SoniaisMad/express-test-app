# Express-test-app

### This is a simple app in express JS with a test made with Jest and Supertest

#### Usage

``` git clone ```

then 

``` npm install ```

Start the server 

``` npm start ```

Run tests

``` npm test ```
